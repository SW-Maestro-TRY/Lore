# 웹툰 생성 파이프라인이 쓰는 파이썬 패키지.
#
# **이 파일 하나로 끝나야 한다.** 배포는 이것만 S3 로 올려서 서버에서 설치한다
# (.github/workflows/deploy.yml → webtoon/be/setup-python.sh). 서버에는 저장소가
# 없고 이 파일만 내려받아 `pip install -r` 로 쓰므로, `-r story-harness/...`
# 처럼 형제 파일을 가리키면 그 자리에서 "그런 파일 없음" 으로 깨진다.
# 그래서 하위 하네스가 쓰는 것까지 여기에 펼쳐 적는다.
#
# 여기 빠지면 **서버에서만** 터진다 — 빌드도 기동도 성공한 채로, 사람이
# 만들기를 눌렀을 때 비로소 죽는다.

# --- 공통 ---------------------------------------------------------------
requests>=2.31
PyYAML>=6.0

# 그림을 다루는 자리. 컷을 한 편으로 이어 붙이고(episode.py), 말풍선·스티커를
# 얹고(strip.py), 올라온 사진과 내려보낼 그림을 줄인다.
# 없으면 1화 합치기가 StitchError 로 멈춘다 — 선택 항목이 아니다.
Pillow>=10.0

# --- 모델 ---------------------------------------------------------------
# 골라 쓰는 것이다 — 실제로 부르는 프로바이더의 것만 있으면 된다.
# story.py 가 셋 다 try/except ImportError 로 감싸고, 그 프로바이더를 실제로
# 부를 때만 "패키지가 없습니다" 하고 멈춘다.
openai>=1.40            # 지금 기본값 (llm.py 의 DEFAULT_PROVIDER)
google-genai>=1.0       # NH_PROVIDER=gemini 로 돌릴 때
# anthropic>=0.34       # NH_PROVIDER=anthropic 을 쓸 때 주석을 푼다

# --- 업로드 -------------------------------------------------------------
# 다 그린 작품을 S3 에 올릴 때 쓴다 (upload/s3_upload.py).
#
# **열쇠는 안 넣는다.** boto3 가 알아서 찾는다 — 서버에서는 EC2 에 붙은
# 역할(lore-ec2-role, s3:PutObject 있음), 로컬에서는 AWS_PROFILE 이나
# 환경변수. 그래서 이 저장소에도 서버에도 키 파일이 없다.
#
# 없어도 서버는 뜬다 — CONTENT_S3_BUCKET 이 비어 있으면 올리는 일 자체를
# 건너뛴다(s3_upload.publish).
boto3>=1.34
