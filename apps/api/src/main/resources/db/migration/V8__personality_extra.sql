-- 성격을 여러 개 고를 수 있게 (2026-09-11 상훈님)
--
-- 상훈님 지시 — "데이터는 많으면 많을수록 좋아. 당장 채팅 구현이 안 되어 있으니까 일단은 이렇게 진행하자"
--
-- ★ 대표는 기존 personality 칸 그대로다. 대사 톤은 그 하나만 본다.
--   여기에는 함께 고른 나머지를 쉼표로 이어 둔다(예: 'SHY,COOL'). 지금은 읽는 곳이 없고,
--   채팅에서 톤을 섞는 방식이 정해질 때 재료로 쓴다.
--
-- ★ 표를 따로 만들지 않은 이유 — 조건으로 걸러 찾을 일도, 하나씩 지울 일도 없는
--   '통째로 읽어 쓰는' 값이다. 쓰임이 정해지기 전에 표를 만들면 유지 비용만 먼저 치른다.
--   5종뿐이라 대표를 뺀 나머지는 아무리 길어도 64자를 넘지 않는다.

ALTER TABLE zzal_pet ADD COLUMN personality_extra character varying(64);
