-- "그날 한 번이라도 돌봤나" 를 기록하는 칸.
--
-- ★ 왜 필요한가 — "케어 미스 0인 날"(웃는 대기 15번의 조건)이 방치한 날에도 잡혔다.
--   케어 미스는 게이지가 채워졌다 다시 0 이 되어야 다음 +1 이 붙는다(원조 다마고치와 같다).
--   그래서 게이지가 이미 전부 0 인 날에는 새 케어 미스가 안 쌓이고, 그 조건만으로는
--   아무것도 안 한 날이 "잘 돌본 날" 로 집계된다. 정본 1.7 에서 "그날 돌봤는가" 를 함께 보게 했다.
--
-- ★ 기존 카운터로는 못 대신한다 — 약은 하루 카운터가 없고, today_care_intimacy 는
--   하루 30 상한에 걸리면 더 안 오르며 간식은 친밀도를 주지 않는다.
--
-- ★ 기존 행은 false 로 시작한다. 오늘 아직 안 돌본 상태로 보는 것이 맞다.

ALTER TABLE zzal_pet ADD COLUMN today_cared boolean DEFAULT false NOT NULL;
