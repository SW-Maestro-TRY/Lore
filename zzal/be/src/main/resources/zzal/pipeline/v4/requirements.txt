# 후처리(격자 → 8종 움짤)가 기대는 것.
#
# ★ 버전을 못박는 이유 — 서버마다 다른 버전이 깔리면 같은 격자에서 다른 결과가 나올 수 있다.
#   이 숫자들은 상훈님 실험 환경(~/work/jakae-lab/01_움짤/.venv)과 같다.
#   실험에서 판정받은 결과와 서비스 결과가 어긋나면 안 되기 때문이다.
numpy==2.5.1
scipy==1.18.0
pillow==12.3.0
