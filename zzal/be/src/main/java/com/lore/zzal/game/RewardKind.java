package com.lore.zzal.game;

/**
 * 무엇을 줄 것인가.
 *
 * ★ 후기와 미니게임이 <b>같은 자리</b>를 쓴다. 둘 다 "무언가 하면 뭘 준다" 는 같은 문제이고,
 *   둘 다 상훈님이 나중에 정하신다(2026-09-03). 각자 지급 로직을 만들면 그때 두 곳을 고쳐야 한다.
 *
 * 지금 기본값은 {@link #NONE} 이다 — 기록만 남고 아무 일도 일어나지 않는다.
 * 정해지면 설정값만 바꾸면 붙는다.
 */
public enum RewardKind {

    /** 아무것도 주지 않는다. 지금 기본값. */
    NONE,

    /** 밥 하나. 4시간 기다리는 것을 건너뛴다. */
    FOOD,

    /** 연습 한 번 분량. */
    TRAIN,

    /** 행복 한 칸. 쓰다듬과 같은 자리. */
    HAPPINESS
}
