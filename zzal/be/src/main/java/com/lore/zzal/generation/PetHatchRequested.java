package com.lore.zzal.generation;

/**
 * "이 펫의 부화를 시작해 달라" 는 알림.
 *
 * ★ 왜 곧바로 부르지 않고 알림을 띄우나 —
 *   펫을 저장하는 트랜잭션이 **아직 커밋되기 전**이기 때문이다.
 *   그 안에서 다른 스레드에게 일을 시키면, 그 스레드가 DB 를 봤을 때 펫이 아직 없을 수 있다.
 *   (있다가 없다가 하는, 재현이 어려운 버그가 된다)
 *
 *   이 알림을 받는 쪽은 **커밋이 끝난 뒤에만** 동작하도록 되어 있어서 그 문제가 생기지 않는다.
 */
public record PetHatchRequested(Long petId) {
}
