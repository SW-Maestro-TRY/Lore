package com.lore.zzal.motion;

/**
 * 이 결과물이 어디서 나왔나.
 *
 * ★ 원가 집계에서 갈라야 한다 — API 는 돈이 나가고 로컬 재생성은 안 나간다.
 *   섞어 세면 사용자당 원가가 실제보다 부풀려진다.
 * ★ 나중에 "로컬에서 다시 구운 것이 정말 더 나은가" 도 이 칸으로 답할 수 있다.
 */
public enum MotionSource {

    /** 운영 서버가 API 로 구웠다(유료). */
    API,

    /** 맥미니에서 다시 구워 올렸다(무과금). */
    LOCAL
}
