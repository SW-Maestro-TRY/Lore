package com.lore.zzal.game;

/**
 * 미니게임 두 종(정본 7장). 두 게임 합쳐 하루 3판, 잠들 때 리셋.
 */
public enum GameKind {

    /** 좌·우 맞히기 — 5판 3승, 답은 서버가 쥔다. 처음부터. */
    LEFT_RIGHT,

    /** 달리기 — 크롬 공룡식 한 버튼. 30초 생존 = 승리. 화면 물리라 서버는 시간 상한만 검증. 좌우 5승에 열림. */
    RUN
}
