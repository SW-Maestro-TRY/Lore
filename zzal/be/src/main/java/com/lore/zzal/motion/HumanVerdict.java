package com.lore.zzal.motion;

/**
 * 상훈님 판정. 비어 있으면 아직 안 보신 것이다.
 *
 * ★ 게이트 판정과 갈라 두는 이유 — 둘을 같은 칸에 쓰면 덮어써져서
 *   "게이트는 통과라 했는데 사람은 재생성이라 한" 건수를 셀 수 없게 된다.
 *   그 건수가 곧 게이트를 강화할 재료다.
 */
public enum HumanVerdict {

    /** 이대로 좋다. */
    OK,

    /** 다시 구워야 한다. */
    REGENERATE
}
