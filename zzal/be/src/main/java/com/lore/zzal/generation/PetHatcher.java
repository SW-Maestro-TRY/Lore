package com.lore.zzal.generation;

/**
 * 알을 부화시키는 사람. 원본 그림에서 캐릭터 시트와 8상태를 만들어 낸다.
 *
 * ★ 인터페이스로 둔 이유 — 굽는 방법이 바뀔 예정이기 때문이다.
 *
 *     FakeHatcher   지금. 실제 생성 없이 단계만 흉내낸다(과금 0). 흐름 검증용
 *     ApiHatcher    다음(#132). OpenAI 를 실제로 호출한다. 캐릭터당 130~138초 · $0.19
 *
 *   펫·부화 로직은 "누가 굽는지" 를 모른다. 구현체만 갈아 끼우면 되고,
 *   그래서 실제 생성을 붙일 때 이 위의 코드는 한 줄도 안 바뀐다.
 *
 * ★ 이 메서드는 **호출 즉시 돌아온다.** 굽는 일은 다른 스레드에서 계속된다.
 *   2분 넘게 걸리는 일을 HTTP 응답으로 붙들고 있으면 브라우저와 ALB 가 먼저 끊어버려,
 *   다 만들어 놓고도 사용자는 실패로 보게 된다.
 */
public interface PetHatcher {

    void hatch(Long petId);
}
