# Lore 환경변수 예시 파일.
# 실제 값은 .env 로 복사해서 채우고, .env 는 git에 커밋하지 않는다.

# --- Web (Next.js) ---
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# --- API (Spring Boot) ---
SERVER_PORT=8080

# --- DB (추후 사용, 지금은 자리만) ---
# DB_URL=jdbc:postgresql://localhost:5432/Lore
# DB_USERNAME=
# DB_PASSWORD=
