# infra

배포/운영 관련 자원을 모아두는 자리.

- **Docker**: Dockerfile / docker-compose (추후 추가)
- **CI/CD**: GitHub Actions 워크플로우 (`.github/workflows/`, 추후 추가)
- **환경변수**: `.env.example` — 실제 값은 `.env` 로 복사해서 사용 (커밋 금지)
