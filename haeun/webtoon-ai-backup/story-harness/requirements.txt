# 텍스트 파이프라인(story.py · webtoon.py)이 실제로 쓰는 것.
requests>=2.31
PyYAML>=6.0

# 아래 셋은 **골라 쓰는 것**이다 — .env 의 PROVIDER 가 가리키는 것만 있으면 된다.
# story.py 는 셋 다 try/except ImportError 로 감싸서 없으면 None 으로 두고,
# 그 프로바이더를 실제로 부를 때만 "openai 패키지가 없습니다" 하고 멈춘다.
openai>=1.40            # PROVIDER=openai (지금 기본값) · 캐릭터 시트 이미지
google-genai>=1.0       # PROVIDER=gemini 로 텍스트를 돌릴 때
# anthropic>=0.34       # PROVIDER=anthropic 을 쓸 때 주석을 푼다
