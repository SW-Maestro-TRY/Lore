# 실행마다 쌓이는 것 — 입력·설정·로그·올린 사진·축소 캐시.
# 산출물 자체는 여기 없습니다 (story-harness/runs, webtoon-harness/outputs 에 남습니다).
jobs/

# new_harness 연결(newharness_pipeline.py)의 job 폴더. 산출물은
# new_harness/runs/ 에 남는다 — jobs/ 와 같은 취급.
jobs_nh/

# 스프링이 직접 만들 때(webtoon/be 의 JobService) 쓰는 job 폴더.
# jobs_nh/ 와 같은 취급 — 사람이 올린 사진과 입력이 들어 있고, 산출물은
# new_harness/runs/ 에 남는다.
jobs_spring/

# 크레딧 잔액·결제 로그 — 서버가 계속 고쳐 쓰는 상태라 jobs/ 와 같은 취급.
data/

__pycache__/
*.pyc
