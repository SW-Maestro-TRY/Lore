# new_harness 의 .env — 이 파일을 .env 로 복사해서 쓰세요.
#
#   cp .env.example .env
#   python3 run.py --plan       # 지금 어느 단계가 어느 모델로 도는지
#
# ─────────────────────────────────────────────────────────────────────
# API 키는 여기 적지 마세요.
#
# new_harness/.env 를 먼저 읽고 story-harness/.env 를 뒤이어 읽는데, 둘 다
# "이미 있는 값은 안 덮어쓴다" 라서 거기 있는 키를 그대로 물려받습니다:
#
#   GEMINI_API_KEY · OPENAI_API_KEY · ANTHROPIC_API_KEY
#   GEMINI_BASE_URL · OPENAI_BASE_URL · ANTHROPIC_BASE_URL
#
# 여기서 정하는 것은 **어느 단계를 어느 모델로 돌릴지** 뿐입니다.
# ─────────────────────────────────────────────────────────────────────
#
# 값 뒤에 주석을 붙이지 마세요 — 주석까지 값으로 읽힙니다
# (story-harness/.env 와 같은 파서입니다).


# ═════════════════════════════════════════════════════════════════════
# 1. 단계
# ═════════════════════════════════════════════════════════════════════
#
#   단계            하는 일                    프로바이더
#   ────────────    ──────────────────────     ─────────────────────────
#   STORY           이야기 후보 4개            gemini · openai · anthropic
#   STORY_REVIEW    이야기 후보 검수 (1회)     gemini · openai · anthropic
#   BOARD           콘티 (JSON)                gemini · openai · anthropic
#   SHEET           캐릭터 시트 사양 (JSON)    gemini · openai · anthropic
#   SHEET_IMAGE     캐릭터 시트 그림           gemini · openai
#   PAGE_IMAGE      페이지 그림 (페이지당 1회)  gemini · openai
#   PAGE_REVIEW     그린 그림 검수 (장당 1회)   gemini · openai · anthropic
#   EPISODE_REVIEW  다 그린 화 검수 (화당 1회)   gemini · openai · anthropic
#
# 값을 고르는 순서 — 위에 있는 것이 이깁니다:
#
#   <단계>_PROVIDER / <단계>_MODEL      이 단계만
#   NH_PROVIDER     / NH_MODEL          이 하네스 전체
#   PROVIDER                            story-harness 와 같은 기본값
#
# 그림 단계는 NH_MODEL 대신 NH_IMAGE_MODEL 을 봅니다 — 글 모델 이름을
# 그림 단계가 물려받으면 그대로 죽기 때문입니다.


# ── 하네스 전체 기본 ─────────────────────────────────────────────────
#
# 글은 GPT, 그림은 Gemini 로 나눠 씁니다.
#   글   — 프로젝트의 다른 하네스도 gpt-4.1 로 돕니다.
#   그림 — Gemini 는 캔버스가 9:16(세로/가로 1.78)까지 되고 seed 가 있습니다.
#          OpenAI 는 1024x1536(1.50)이 가장 세로로 긴 값이라 세로 웹툰에
#          16% 짧고, seed 가 아예 없어서 그림체가 흔들려도 원인을 못 잡습니다.
NH_PROVIDER=openai

# 비우면 프로바이더의 기본 모델을 씁니다 (story-harness/.env 의
# GEMINI_MODEL / OPENAI_MODEL 을 물려받습니다).
# NH_MODEL=


# ── 단계마다 다른 모델 (필요한 줄만 켜세요) ──────────────────────────
#
# 예: 발상이 필요한 이야기 후보만 GPT 로 뽑고, 형식을 지켜야 하는 콘티는
#     Gemini 로 두고, 시트 사양은 Claude 로 —
#
# STORY_PROVIDER=openai
# STORY_MODEL=gpt-5.1
#
# BOARD_PROVIDER=gemini
# BOARD_MODEL=gemini-3-pro-preview
#
# SHEET_PROVIDER=anthropic
# SHEET_MODEL=claude-opus-5


# ── 이야기 후보 검수 (고르기 전에 한 번 본다) ────────────────────────
#
# 후보 4개를 다 만든 뒤, 처음 읽는 독자의 눈으로 한 번 더 읽습니다. 어느
# 후보가 나은지는 **안 봅니다**(그건 사람이 고르는 자리입니다) — "이 후보를
# 그대로 그리면 독자가 어디서 걸리는가" 만 봅니다. 판정은 고르는 화면의
# 카드 옆에 붙고, **아무 후보도 막지 않습니다.**
#
#   NH_STORY_REVIEW=0       검수를 끕니다 (기본은 켜짐)
#
# 글만 읽는 호출이라 눈은 없어도 됩니다. 비우면 NH_MODEL 을 물려받습니다.
# NH_STORY_REVIEW=1
# STORY_REVIEW_PROVIDER=
# STORY_REVIEW_MODEL=


# ── 그림 검수 (그린 뒤 한 번 더 본다) ────────────────────────────────
#
# 한 장을 그릴 때마다, **직전 장과 지금 장 두 그림**을 이야기와 함께 모델에게
# 주고 "앞 장에서 이어지는가" 를 묻습니다. `제자리`(같은 순간을 다시 그림) ·
# `건너뜀`(안 그린 장면을 통째로 지나침) · `되돌아감` 이 나오거나 critical 이
# 걸리면 그 장을 지우고 다시 그립니다.
#
#   NH_PAGE_REVIEW=0        검수를 끕니다 (기본은 켜짐)
#   NH_PAGE_REVIEW_RETRY=0  검수는 하되 다시 그리지는 않습니다 (기본 1회)
#
# ★ 그림을 보는 호출이라 **눈이 있는 모델이어야 합니다.** 비우면 위 NH_MODEL
#   을 물려받습니다.
# NH_PAGE_REVIEW=1
# NH_PAGE_REVIEW_RETRY=1
# PAGE_REVIEW_PROVIDER=gemini
# PAGE_REVIEW_MODEL=


# ── 화 전체 검수 (다 그린 뒤 처음부터 읽는다) ────────────────────────
#
# 이 화를 다 그리고 나서, 페이지 **전부**를 한 번에 주고 "아무것도 모르는
# 사람이 이 화면들만 넘겨 보고 이해하는가" 를 묻습니다. 장마다 보는 검수는
# 인접한 두 장만 보므로, 장장이 매끄러운데 다 읽고 나면 무슨 이야기였는지
# 모르겠는 것은 거기서 안 잡힙니다.
#
# **다시 그리지 않습니다.** 판정만 남기고 완성본 화면에 보여 줍니다 — 그래서
# 장마다 보는 검수가 일부러 안 보는 것(깨진 글자)도 여기서는 봅니다.
#
#   NH_EPISODE_REVIEW=0     검수를 끕니다 (기본은 켜짐)
#
# ★ 그림을 보는 호출이라 **눈이 있는 모델이어야 합니다.** 비우면 NH_MODEL 을
#   물려받습니다.
# NH_EPISODE_REVIEW=1
# EPISODE_REVIEW_PROVIDER=gemini
# EPISODE_REVIEW_MODEL=


# ── 그림 ─────────────────────────────────────────────────────────────
#
# ★ 프로바이더별 기본 이미지 모델을 **여기서 못 박습니다.**
#   story-harness/.env 에는 이 두 줄이 없어서, 안 적으면 story.py 의
#   맨 옛날 기본값(gemini-2.5-flash-image)으로 조용히 떨어집니다.
#   프로젝트의 다른 하네스(webtoon-harness/.env)가 쓰는 값과 맞춰 둡니다 —
#   같은 작품인데 하네스마다 다른 모델로 그리면 그림체가 갈립니다.
GEMINI_IMAGE_MODEL=gemini-3-pro-image-preview
OPENAI_IMAGE_MODEL=gpt-image-2

# 어느 쪽으로 그릴지. gemini | openai
SHEET_IMAGE_PROVIDER=openai
PAGE_IMAGE_PROVIDER=openai

# 단계마다 다른 모델을 쓰고 싶을 때만 켭니다. 비우면 위의 프로바이더별
# 기본값을 씁니다.
#
# ★ 시트와 페이지를 **다른 모델로 두지 마세요.** 페이지를 그릴 때 시트를
#   참조 이미지로 붙이는 구조라, 시트를 그린 모델과 페이지를 그리는 모델이
#   다르면 참조가 오히려 그림체를 흔듭니다.
# SHEET_IMAGE_MODEL=
# PAGE_IMAGE_MODEL=

# 그림체. prompt/style/<이름> 파일을 그대로 씁니다. 새 그림체를 만들려면
# 그 폴더에 파일 하나를 더 놓으면 됩니다. (2026-08-30 기준: frost · romance ·
# webtoon · romance_fantasy · webtoon_lock. 기본은 webtoon_lock — 깔끔한 선화 +
# 평면 색면 + 얕은 셀 명암 + 웹툰식 생략. 자세한 내용은
# new_harness/STYLE_FINDINGS.md 참고.)
#
# ★ 시트와 페이지가 **같은 값을 씁니다.** 갈라지면 시트가 다른 그림체로
#   그려지고, 그 시트가 매 페이지에 참조로 붙어서 페이지 프롬프트를 이깁니다.
#   그림체를 바꾸면 시트부터 다시 뽑아야 합니다.
NH_STYLE=webtoon_lock_bg

# 페이지 캔버스. 한 페이지에 컷을 몇 개까지 모을지가 이 값에서 자동으로
# 나옵니다 (세로가 길수록 더 많이). OpenAI 는 픽셀만 받고 셋뿐입니다
# (1024x1024 · 1024x1536 · 1536x1024). Gemini 는 비율이고 9:16 이 천장입니다.
PAGE_CANVAS_OPENAI=1024x1536
PAGE_CANVAS_GEMINI=9:16

# 프롬프트도 프로바이더마다 따로입니다 — 캔버스 모양과 참조 이미지를 부르는
# 방식이 달라서입니다. prompt/image_prompt.<provider> 를 고치면 됩니다.
#   prompt/image_prompt.openai
#   prompt/image_prompt.gemini

# openai 로 그릴 때만 봅니다. low | medium | high
# medium 만 저장소에 단가가 적혀 있습니다($0.041/장). 다른 품질은
# 요금표가 없어서 비용 표시가 거짓말이 됩니다.
OPENAI_IMAGE_QUALITY=medium


# ═════════════════════════════════════════════════════════════════════
# 2. 공통
# ═════════════════════════════════════════════════════════════════════

# 한 호출의 최대 출력 토큰. 콘티가 길면 여기서 끊깁니다 — 끊기면
# "응답이 길이 제한에서 끊겼습니다" 가 뜹니다.
NH_MAX_TOKENS=16000

# 창작 온도. 시트 사양만 코드가 0.4 로 낮춰 부릅니다 (사양은 발상이 아니라
# 받아쓰기라, 매번 달라지면 곤란합니다).
NH_TEMPERATURE=0.9

# 사진 한 장의 최대 크기(MB). 넘으면 줄여서 다시 넣으라고 멈춥니다.
# MAX_IMAGE_MB=5
