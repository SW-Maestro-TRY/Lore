# 그림체 실험 기록 (2026-08-30)

시작점은 사용자 피드백 한 줄이다 — **"그림체가 너무 AI티가 난다."**
하루 동안 그림체 문구를 다섯 번 고쳐 여섯 장을 뽑았다. 무엇이 먹혔고
무엇이 안 먹혔는지, 그리고 **다시 하지 말아야 할 것**이 아래에 있다.

이어서 작업할 때는 이 문서를 먼저 읽고, 결론 부분("아직 안 갈린 것")부터
손대라. 그림체 문구를 또 길게 다시 쓰는 것은 이미 네 번 해봤고 네 번 다
같은 벽에 부딪혔다.

## 지금 상태

| | |
| --- | --- |
| 그림체 파일 | `prompt/style/` 에 `frost` · `romance` · `webtoon` |
| 고르는 법 | `.env` 의 `NH_STYLE`, 또는 `NH_STYLE=<이름> python3 run.py ...` |
| 이미지 모델 | `gpt-image-2` (openai), `OPENAI_IMAGE_QUALITY=medium` |
| 실험 결과물 | `runs/로판테스트/` 에 여섯 장이 번호순으로 |
| 실험용 run | `runs/style-frost` · `style-frost2` · `style-frost3` · `style-romance` |

실험은 전부 **모모(로맨스 판타지) 1페이지** 한 컷으로 했다 —
`runs/20260829T222230-9aa58a` 의 `pages.json` · `sheet.png` 를 복사해
`--page 1` 만 다시 그렸다. 컷 내용은 축제 광장에 꽃 장식 뒤에 선 모모.

## 세 그림체가 뭐가 다른가

한 파일 안에 두 그림체가 섞여 있던 것을 갈라 놓은 결과다.

- **`frost`** — 일러스트 한 장의 마감. 셀 셰이딩이 아니라 반사광·환경광까지
  넣는다. 찬 색, 탁한 채색, 반실사 얼굴, 머리카락 극세밀. 색보정 안 함.
- **`romance`** — 그리는 밀도는 frost 와 같고 **색만 다르다.** 장르 색감과
  보정을 한 겹 더 씌운다 (한 장면 한 색 화음, 채도 올림, 밝은 곳 따뜻하게
  그림자 차갑게).
- **`webtoon`** — 반대 방향. 덜 그리는 것이 그림체다. 셀 셰이딩, 넓은 단색
  면, 배경은 덩어리로, 먼 것은 평평한 실루엣 하나, 한 컷에 한 동작.

## 뽑아 본 것

`runs/로판테스트/` 의 파일 번호와 같다.

| # | 무엇을 바꿨나 | 결과 |
| --- | --- | --- |
| 0 | 원본 `frost` (찬 색·셀셰이딩·잉크선) | 쨍한 분홍 에어브러시 가챠 일러스트. **문구와 정반대** |
| 1 | `romance` 이식 (반짝임을 줄이는 방향) · gpt-image-2 | 구도·배경 밀도는 좋아짐. 마감은 여전히 에어브러시 |
| 2 | 같은 문구 · gemini-3-pro-image | **더 나쁨.** 컷 하나를 두 컷으로 쪼갬, 전면 글로우, 배경에 인물이 묻힘 |
| 3 | `frost` 화려한 쪽으로 (또렷한 반짝임·머리카락 가닥) | 머리카락·속눈썹·선이 확실히 좋아짐. 대신 **웹툰풍**이 됨 |
| 4 | `romance` 같은 방향 | 3번과 같음. 꽃·장식이 더 빽빽 |
| 5 | `frost` 일러스트 마감 + 반실사 얼굴 규정 | 머리카락만 더 좋아짐. **얼굴 안 바뀜** |
| 6 | `frost` 탁한 채색 + "AI 특유의 채색" 금지 목록 | **색 거의 안 바뀜.** 사용자 평: 5번보다 못하다 |

## 확정된 것

**1. 모델 탓이 아니다.** `gpt-image-2` 가 `gemini-3-pro-image` 보다 낫다
(2번). Gemini 는 페이지 구조(컷 개수)도 틀렸다. 모델을 바꿔서 해결하려
하지 마라.

**2. "AI티 난다" 를 반짝임·장식을 줄이라는 뜻으로 읽으면 안 된다.**
처음에 그렇게 읽고 `frost` 를 셀 셰이딩 쪽으로 눌렀다가 정반대라는 지적을
받았다. 원하는 것은 **속눈썹 길고 머리카락 묘사 빡세고 눈도 머리도 반짝이는
화려한 일러스트**다. 화려한 그림과 AI 그림은 반짝임의 **양**이 아니라
**또렷함**에서 갈린다 — 자리와 모양이 있는 반짝임은 많을수록 좋고, 자리
없이 화면에 깔린 번짐은 하나만 있어도 AI로 보인다.

**3. 그림체 문구로 먹히는 것과 안 먹히는 것이 갈린다.**

| 먹힌다 | 안 먹힌다 |
| --- | --- |
| 구도 · 앵글 | **얼굴 구조** (눈 크기, 코, 턱선) |
| 배경 밀도와 덩어리 짓기 | **채도 · 색온도** |
| 머리카락 가닥 수 | 마감의 부드러움(에어브러시) |
| 선 굵기 · 장식 밀도 | |

**4. 같은 하네스가 인물에 따라 전혀 다른 그림을 낸다.**

- **박하은** (`runs/20260829T195833-da85ca/pages/page01.png`) — 원본 `frost`
  로 뽑았는데 탁한 회녹색, 반실사 얼굴, 헝클어진 머리 가닥. **사용자가
  원하는 그림이 이것이다.**
- **모모** — 그림체를 다섯 번 바꿔도 계속 쨍한 분홍 + 가챠 얼굴.

두 인물의 차이는 둘뿐이다: **캐릭터 시트 그림**과 **컷 설명의 장소·시간대**.
박하은 시트(`runs/20260829T195833-da85ca/sheet.png`)는 눈이 가로로 길고
코와 턱선이 그려진 반실사 얼굴이고, 모모 시트는 얼굴 절반을 덮는 둥근 눈에
코가 없는 가챠 얼굴이다. 시트는 **매 호출에 참조로 붙고**, `prompt/image_prompt`
에 "첨부한 캐릭터 시트의 외관을 그대로 따른다" 가 박혀 있다.

## 아직 안 갈린 것 — 여기서부터 하면 된다

**얼굴과 채색을 실제로 정하는 것이 무엇인가?** 후보가 셋인데 아직 안 갈랐다.

1. 참조로 붙는 **캐릭터 시트**
2. 컷 설명의 **장소·시간대** (모모 1페이지는 "맑은 낮, 햇빛과 화려한 장식
   조명" 이다)
3. 인물 자체의 색 (분홍 머리·분홍 망토라 화면이 저절로 분홍이 된다)

싼 순서로 이렇게 가르면 된다. 전부 호출 1~2회짜리다.

- **`--no-sheet` 로 모모 1페이지를 한 장.** 시트를 안 붙이고 그렸는데 색과
  얼굴이 바뀌면 1번이 범인이다. 한 번에 갈린다. (호출 1회)
- **모모 시트를 지금 `frost` 로 다시 뽑고, 그 시트로 컷을 그린다.** 위에서
  1번이 범인으로 나오면 이것이 곧 해결책이다. (호출 2회)
- **컷 설명의 시간대를 어두운 쪽으로 바꿔서 한 장.** 2번을 가른다. (호출 1회)

## 다시 하지 마라

- **그림체 문구를 더 길게 써서 얼굴과 채색을 고치려는 것.** 네 번 했고 네
  번 다 안 됐다 (3·5·6번). 문구는 이미 충분히 구체적이다 — "눈은 얼굴
  가로폭의 5분의 1", "순색을 쓰지 않는다" 까지 적혀 있는데도 안 먹었다.
  지렛대가 다른 데 있다.
- **모델을 바꿔 보는 것.** 2번에서 이미 했다.
- **`frost` 에 웹툰 문구를 다시 섞는 것.** 원래 한 파일에 "로판 일러스트의
  밀도로 그린다" 와 "칠은 셀 셰이딩이다 / 평평한 색면이 넓게 남는 것이 맞다"
  가 같이 들어 있었고, 뒤엣말이 이겨서 일러스트를 고르면 웹툰이 나왔다.
  평면 쪽이 필요하면 `webtoon` 을 쓴다.
- **`frost` 를 지우거나 `romance` 로 합치는 것.** 둘은 색으로 갈리는 별개
  그림체이고, 사용자가 둘 다 쓰겠다고 명시했다.

## 사용자가 말한 것 (원문)

의도를 다시 읽을 때 쓰라고 남긴다.

- "그림체 최대한 ai티 안나게 예쁜 네이버 웹툰 일러스트풍 로판풍 조명도 좀
  쓰고 화사하게 그런 꽃배경이라든지"
- "약간 일러스트 풍 느낌으로 속눈썹 길고 머리카락 묘사 빡세게 들어가고
  화려하고 반짝반짝 머리도 눈도 반짝거리는 느낌 … 선도 깔끔하고 얇고"
- "로맨스 판타지 장르는 좀더 색감이나 보정이 많이 들어가고 frost는 그림체만
  이쁜느낌으로"
- "지금 frost 너무 일러스트가 아니라 웹툰풍이야" / "웹툰그림체 따로있어"
- "얼굴형 자체를 얼굴 자체를 약간 좀 다른 느낌으로 속눈썹 길고 실사까진
  아니더라도"
- "내 생각엔 이거 채색법이 문제야 … frost그림체는 채색을 최대한 탁한느낌으로
  채도를 [낮춰서]" / "지금 ai 특유의 채색이 문제인거같아"

## 추가 — `romance_fantasy` · `webtoon_lock` (2026-08-30, 같은 날 이어서)

사용자가 ChatGPT로 직접 설계한 그림체 프롬프트 두 개를 파일로만 추가했다.
**둘 다 이 하네스(`gpt-image-2`)로 실제 렌더링 테스트를 한 번도 안 거쳤다** —
그대로 믿고 쓰지 말고, 다음에 만지는 사람이 먼저 검증부터 하라.

- **`romance_fantasy`** — 저채도·저대비·하이키 파스텔 방향. **이름이
  `romance`와 비슷하지만 완전히 다른 별개 파일이니 헷갈리지 말 것.** **위
  "확정된 것 3번"과 정면충돌한다** — `romance` 파일은 하이키·저대비를 "AI
  특유의 채색"으로 명시해 뒀다. 사용자도 이후 메시지에서 "핵심은 파스텔이
  아니다"로 스스로 정정했다. **참고용으로만 남기고, 다음 실험의 기준으로
  쓰지 마라.**
- **`webtoon_lock`** — 사용자의 최종 결론. 깔끔한 선화 + 평면 색면 + 얕은
  셀 명암 + 웹툰식 생략이 핵심이고 색은 장면마다 달라져도 된다(로맨스는
  파스텔, 공포는 어두운 색 등). 기존 `webtoon` 과 방향이 겹치지만 훨씬
  정교하고, "장면 프롬프트가 그림체를 덮어쓰면 안 된다"는 경고가 못박혀
  있다. **`DEFAULT_STYLE`·`.env`·`.env.example` 의 기본값을 이걸로 바꿨다**
  (사용자 지시, 이 하네스로 실측하기 전임을 감안할 것).

**다음에 이어서 할 때 반드시 먼저 할 것:**

1. 기존 `sheet.png` 들은 전부 `frost` 로 그려져 있다. 위 "★ 시트와 페이지가
   같은 값을 씁니다" 경고대로, `webtoon_lock` 으로 페이지를 그리기 전에
   시트부터 `webtoon_lock` 으로 다시 뽑아야 한다 — 안 그러면 시트가 참조로
   붙어서 여전히 frost 가 나올 수 있다 (위 "아직 안 갈린 것" 절의 가설과
   같은 문제).
2. 모모 1페이지(`runs/로판테스트/` 와 같은 셋업)로 `webtoon_lock` 을 한 장
   뽑아 기존 6장과 나란히 비교한다 — 승인 없이 실제 생성을 돌리지 않는다.
3. 결과가 나쁘면 `frost` 로 기본값을 되돌리는 것을 망설이지 마라 — 이 절의
   변경은 아직 근거(이 하네스 안에서의 실제 결과)가 없는 상태에서 이뤄졌다.

## 커밋

- `c52fd79` romance 를 new_harness 로 이식
- `8af2efd` 화려한 쪽으로 되돌림, frost·romance 를 색으로 가름
- `8976ef8` frost 를 일러스트 마감으로, webtoon 신설, 반실사 얼굴
- `189f582` 채색 절 신설 (탁하게 · AI 특유의 채색 목록) ← **사용자 평이
  안 좋다.** 되돌릴지 판단이 필요하다
